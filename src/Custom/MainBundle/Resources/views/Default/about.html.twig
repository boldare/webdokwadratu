{% extends '::base.html.twig' %}
{% set menu_about = 'active' %}

{% block content %}
    <div class="content" role="content">
    	<h2>{{ article.title}}</h2>
        {{ article.content | raw }}
    </div>
{% endblock %}