{% extends '::base.html.twig' %}
{% set menu_album = 'active' %}

{% block content %}
    <div class="album content" role="content">
    	<h2>{{ article.title}}</h2>
        {{ article.content | raw }}
    </div>
{% endblock %}