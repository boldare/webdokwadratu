{% extends "::base.html.twig" %}

{% block content %}
    {% include "MainBundle:Default:admin_menu.html.twig" %}
    <h1>{{ 'Computer list' | trans }}</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>{{ 'Name' | trans }}</th>
                <th>{{ 'Actions' | trans }}</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td>{{ entity.name }}</td>
                <td>
                    <ul>
                        <li>
                            <a href="{{ path('computer_show', { 'id': entity.id }) }}">{{ 'show' | trans }}</a>
                        </li>
                        <li>
                            <a href="{{ path('computer_edit', { 'id': entity.id }) }}">{{ 'edit' | trans }}</a>
                        </li>
                    </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('computer_new') }}">
                {{ 'Create a new entry' | trans }}
            </a>
        </li>
    </ul>
{% endblock %}
